@model ProductViewModel

<div class="card shadow border-0 mt4">
    <div class="card-header bg-secondary bg-gradient ml-0 py-3">
        <div class="row">
            <div class="col-12 text-center">
                <h2 class="text-white py-2">@(Model.Product.Id == 0 ? "Create" : "Update") Product</h2>
            </div>
        </div>
    </div>
    <div class="card-body p4">
        <form class="row" method="POST" enctype="multipart/form-data">
            <input type="hidden" asp-for="Product.Id" />
            <div class="border p-3">
                @*
                In addition to asp-validation-for for individual fields we also have
                asp-validation-summary for general validation messages.
                It will display all error messages generated in a bulleted list.
                Three flavors are available
                1. asp-validation-summary="All". Displays all error messages.
                2. asp-validation-summary="ModelOnly". Does not display property related validations. That any
                errors
                that have the key assigned for the ModelState
                3. asp-validation-summary="None". Displays no errors
                *@
                <div asp-validation-summary="ModelOnly"></div>
                <div class="form-floating py-2 col-12">
                    @*
                    asp-for is what connects our html to our model fields
                    Note below we do not even have to specify text for label
                    It pulls it directly from the model annotations or the model field itelf if no annotation is
                    present
                    *@
                    <input asp-for="Product.Title" class="form-control border-0 shadow" />
                    <label asp-for="Product.Title" class="ms-0"></label>
                    <span asp-validation-for="Product.Title" class="text-danger"></span>
                </div>
                <div class="form-floating py-2 col-12">
                    @*
                    asp-for is what connects our html to our model fields
                    Note below we do not even have to specify text for label
                    It pulls it directly from the model annotations or the model field itelf if no annotation is
                    present
                    *@
                    <textarea asp-for="Product.Description" class="form-control border-0 shadow"></textarea>
                    <label asp-for="Product.Description" class="ms-0"></label>
                    @* <span asp-validation-for="Product.Description" class="text-danger"></span> *@
                </div>
                <div class="form-floating py-2 col-12">
                    @*
                    asp-for is what connects our html to our model fields
                    Note below we do not even have to specify text for label
                    It pulls it directly from the model annotations or the model field itelf if no annotation is
                    present
                    *@
                    <input asp-for="Product.ISBN" class="form-control border-0 shadow" />
                    <label asp-for="Product.ISBN" class="ms-0"></label>
                    <span asp-validation-for="Product.ISBN" class="text-danger"></span>
                </div>
                <div class="form-floating py-2 col-12">
                    @*
                    asp-for is what connects our html to our model fields
                    Note below we do not even have to specify text for label
                    It pulls it directly from the model annotations or the model field itelf if no annotation is
                    present
                    *@
                    <input asp-for="Product.Author" class="form-control border-0 shadow" />
                    <label asp-for="Product.Author" class="ms-0"></label>
                    <span asp-validation-for="Product.Author" class="text-danger"></span>
                </div>
                <div class="form-floating py-2 col-12">
                    @*
                    asp-for is what connects our html to our model fields
                    Note below we do not even have to specify text for label
                    It pulls it directly from the model annotations or the model field itelf if no annotation is
                    present
                    *@
                    <input asp-for="Product.ListPrice" class="form-control border-0 shadow" />
                    <label asp-for="Product.ListPrice" class="ms-0"></label>
                    <span asp-validation-for="Product.ListPrice" class="text-danger"></span>
                </div>
                <div class="form-floating py-2 col-12">
                    @*
                    asp-for is what connects our html to our model fields
                    Note below we do not even have to specify text for label
                    It pulls it directly from the model annotations or the model field itelf if no annotation is
                    present
                    *@
                    <input asp-for="Product.Price" class="form-control border-0 shadow" />
                    <label asp-for="Product.Price" class="ms-0"></label>
                    <span asp-validation-for="Product.Price" class="text-danger"></span>
                </div>
                <div class="form-floating py-2 col-12">
                    @*
                    asp-for is what connects our html to our model fields
                    Note below we do not even have to specify text for label
                    It pulls it directly from the model annotations or the model field itelf if no annotation is
                    present
                    *@
                    <input asp-for="Product.Price50" class="form-control border-0 shadow" />
                    <label asp-for="Product.Price50" class="ms-0"></label>
                    <span asp-validation-for="Product.Price50" class="text-danger"></span>
                </div>
                <div class="form-floating py-2 col-12">
                    @*
                    asp-for is what connects our html to our model fields
                    Note below we do not even have to specify text for label
                    It pulls it directly from the model annotations or the model field itelf if no annotation is
                    present
                    *@
                    <input asp-for="Product.Price100" class="form-control border-0 shadow" />
                    <label asp-for="Product.Price100" class="ms-0"></label>
                    <span asp-validation-for="Product.Price100" class="text-danger"></span>
                </div>
                <div class="form-floating py-2 col-12">
                    @*
                    asp-for is what connects our html to our model fields
                    Note below we do not even have to specify text for label
                    It pulls it directly from the model annotations or the model field itelf if no annotation is
                    present
                    *@
                    @*
                    ViewBag
                    Using ViewBag to populate a dropdown list.
                    Note CategoryList is the key to access the data in the ViewBag/ViewData.
                    It is a key we made up. It can be any string.
                    <Select asp-for="CategoryId" asp-items="ViewBag.CategoryList" class="form-select border-0
                    shadow"></select>
                    *@
                    @*
                    ViewData Example
                    Using ViewData to populate dropdown list
                    Note we need to cast the data in ViewData before use
                    See commented out example immediately below
                    <Select asp-for="CategoryId" asp-items="@(ViewData["CategoryList"] as
                    IEnumerable<SelectListItem>)"
                    class="form-select border-0 shadow"></Select
                    *@
                    @*
                    Finally we opted to use ViewModel. Need to create the model first.
                    Then use it in the controller and at the top of this page.
                    Then must adjust asp-for to work with the model.
                    Must alos set asp-items to use the model
                    *@
                    <Select asp-for="Product.CategoryId" asp-items="@Model.CategoryList"
                        class="form-select border-0 shadow">
                        <Option disable selected>--Select Category--</Option>
                    </Select>
                    <label asp-for="Product.CategoryId" class="ms-0"></label>
                    <span asp-validation-for="Product.CategoryId" class="text-danger"></span>
                </div>
                <div class="form-floating py-2 col-12">
                    <input type="hidden" asp-for="Product.ImageUrl" />
                    <img src="@Model.Product.ImageUrl"
                        style="object-fit:cover; width:100%; height:100%; overflow:hidden; border-radius:5px; border:1px solid #bbb9b9" />
                </div>
                <div class="form-floating py-2 col-12">
                    @*
                    asp-for is what connects our html to our model fields
                    Note below we do not even have to specify text for label
                    It pulls it directly from the model annotations or the model field itelf if no annotation is
                    present
                    *@
                    @*
                    for file uploads controller expects submission to have a name.
                    so we supply a name attribute. It can be anything. In this case we
                    called it "file"
                    *@
                    <input type="file" name="file" class="form-control border-0 shadow" />
                    <label asp-for="Product.ImageUrl" class="ms-0"></label>
                </div>
                <div class="row mt-3">
                    <div class="col-6 col-md-3 p-1">
                        @if (Model.Product.Id == 0)
                        {
                            <button class="btn btn-primary form-control" type="submit">Create</button>
                        }
                        else
                        {
                            <button class="btn btn-primary form-control" type="submit">Update</button>
                        }
                    </div>
                    <div class="col-6 col-md-3 p-1">
                        <a class="btn btn-outline-primary border form-control" asp-area="Admin" asp-controller="Product"
                            asp-action="Index">Products</a>
                    </div>
                </div>
            </div>
        </form>
    </div>
</div>



@*
Add client-side validation in addition to the server-side validation above
Client-side valididation is implemented using jquery from the partial scripts files
in the shared views. The syntax below is generally for consuming a partial view.
*@
@section Scripts {
    <script>
        tinymce.init({
            selector: 'textarea',
        @* plugins: 'ai tinycomments mentions anchor autolink charmap codesample emoticons image link lists media searchreplace table visualblocks wordcount checklist mediaembed casechange export formatpainter pageembed permanentpen footnotes advtemplate advtable advcode editimage tableofcontents mergetags powerpaste tinymcespellchecker autocorrect a11ychecker typography inlinecss', *@
        @* toolbar: 'undo redo | blocks fontfamily fontsize | bold italic underline strikethrough | link image media table mergetags | align lineheight | tinycomments | checklist numlist bullist indent outdent | emoticons charmap | removeformat', *@
            plugins: 'ai tinycomments mentions anchor autolink charmap codesample emoticons lists table visualblocks wordcount checklist mediaembed casechange export formatpainter pageembed permanentpen footnotes advtemplate advtable advcode editimage tableofcontents mergetags powerpaste tinymcespellchecker autocorrect a11ychecker typography inlinecss',
            toolbar: 'undo redo | blocks fontfamily fontsize | bold italic underline strikethrough | align lineheight | tinycomments | checklist numlist bullist indent outdent',
            tinycomments_mode: 'embedded',
            tinycomments_author: 'Author name',
            mergetags_list: [
            { value: 'First.Name', title: 'First Name' },
            { value: 'Email', title: 'Email' },
        ],
            ai_request: (request, respondWith) => respondWith.string(() => Promise.reject("See docs to implement AI Assistant")),
                                                                                    });
    </script>
    @{
        <partial name="_ValidationScriptsPartial" />
    }
}